[project]
name = "nba-scores-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi[standard]",
  "uvicorn[standard]",
  "nba_api",
  "requests",
  "beautifulsoup4",
  "numpy",
  "pandas",
]

[tool.fastapi]
entrypoint = "server.main:app"
